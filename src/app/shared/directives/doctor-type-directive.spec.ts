import { DoctorTypeDirective } from './doctor-type-directive';

describe('DoctorType', () => {
  it('should create an instance', () => {
    const directive = new DoctorTypeDirective();
    expect(directive).toBeTruthy();
  });
});
